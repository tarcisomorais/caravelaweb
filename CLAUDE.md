@AGENTS.md

## Claude Code

Use the project-local `caravelaweb` skill for web tasks. Claude Code discovers it
from `.claude/skills/caravelaweb/` in this checkout, so no user-level install is
needed. The repository-root `SKILL.md` stays the canonical contract, and the
runtime always resolves from the repository root.
