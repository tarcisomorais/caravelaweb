# Implementation Plans

Generated by the improve skill on 2026-09-02 against commit `929c0b1`
(release 0.1.0). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

The user ran this audit non-interactively, so plans were written for the
findings with the highest leverage by default. Everything else is recorded
in the backlog and rejection sections so it is not re-audited.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Surface pending Candidates in lookup and name conflicting Claims | P1 | M | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 002 | `knowledge-resolve --reject-pending` to discard a stuck Candidate | P1 | M | 001 (soft) | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 003 | Make the `OPERATIONAL` lifecycle reachable and visible | P1 | M | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 004 | Name accepted sets and add reason codes to every refusal | P1 | M | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 005 | Host association by default and IDNA-safe evidence matching | P2 | M | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 006 | Canonical timestamps, short-form IP literals, run-marker symlink guard | P2 | S | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 007 | `knowledge-lookup --list` memory index | P2 | S | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |
| 008 | Correct stale public documentation claims | P2 | S | — | DONE (reviewed 2026-09-02; one commit on `advisor/plans-2026-09-02`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

All eight plans landed on branch `advisor/plans-2026-09-02` (worktree
`../caravelaweb-advisor`, nine commits, one per plan plus this directory,
366 tests OK). Merging into `main`
is the maintainer's decision. Reviewed deviations: plan 002 also catches
`WriteAuthorityStateError`; plan 004 updated three tests that pinned old
refusal text; plan 005 added `host` + `TARGET_SURFACE` scope to examples 5-8
(not only 1-3) because the example loop test now asserts no `warnings`;
plan 006 keeps the `os.lstat` pre-check beside the descriptor-first read so
`test_marker_parity` mocks still hold.

## Dependency notes

- 002 consumes the `proposal_id` that 001 exposes in lookup output. 002 can
  land first (its tests fall back to the Python API), but the user-facing
  flow only makes sense once 001 shows the pending state.
- 001 and 004 both edit the `NOT_SAVED` reason text in
  `discovery_finalize.py` on different lines; merge in either order.
- 003, 005, 006 touch `discovery_finalize.py` / `core.py` in disjoint
  functions; merge in any order, rerun the full suite after each.
- 008 changes "four entry points" to "five"; if 002 landed first, write
  "six" and include `knowledge-resolve`.

## Health check of the maintainer's memory (2026-09-02)

Checked read-only on a copy of `~/.local/share/caravelaweb/knowledge-root`:

- `preflight` reports `READY`; SQLite `integrity_check` ok; no WAL/lock
  residue; no orphan Claims; no open Discovery markers.
- 35 targets, 48 capabilities, 181 Claims, 46 Decisions (all `ACCEPT`),
  8 pending Proposals across 6 capabilities. Activity 2026-08-13 to 2026-09-02.
- 0 lifecycle Claims: no capability has ever reached `OPERATIONAL` (plan 003).
- 2 capabilities are unwritable because of pending conflicts
  (`financial-times/article-full-text-access`, `bloomberg/international-geopolitics-rss-feed`), which caused a duplicate capability to be minted (plans 001, 002).
- 16 of 35 targets have no host association and cannot be found by URL (plan 005, 007).
- A stale `~/.local/share/caravelaweb/config` file from the retired
  persisted-pointer design is present; nothing reads it and the documented
  uninstall removes it. Not a finding.

## Backlog (found, vetted, not planned in this run)

- **SECURITY-02** No credential filter on free-text fields (`condition`,
  `entrypoint`, `evidence.locator`); `references/safety.md` forbids secrets
  in profiles but nothing enforces it. Needs a design decision on
  false-positive tolerance before planning. Effort M, risk MED.
- **CORRECTNESS-04** Check-then-act around the run marker in
  `scripts/discovery-finalize` (two concurrent finalizes for one `run_id`).
  Portable lock needed (fcntl absent on Windows). Effort M.
- **CORRECTNESS-05** `initialize_knowledge_root` rollback unlinks files but
  leaves the directories it created, so a failed init still validates as a
  root. Effort S.
- **DEBT-01** Legacy `targets/*.md` lookup path still reachable via
  `--use-legacy` and by a missing read-cutover marker; also a path-traversal
  vector (SECURITY-01, low impact: the marker is always present after
  `init-knowledge-root`). Retire the branch. Effort S–M.
- **DEBT-04 / TESTS-01/03/04/05** `finalize_discovery` (339 lines) and
  `capture_candidate` (17 parameters) need characterization tests before
  any split; transport-trace refusal branches and `om_native_writes` guard
  paths are measured at 0 coverage. Write the tests first (TESTS-05 pairs
  with plan 004's reason codes).
- **TESTS-02 / TESTS-06** Discovery tests only run under the migrated
  authority kind; the authority-marker fixture is copied in 8 files. Extract
  one helper and parametrize both kinds. Effort S.
- **DX-01** No lint/format/typecheck config; **DEPS-01** CI tests only
  Python 3.11 while the floor is "3.11 or newer". Add ruff/mypy baseline and
  a 3.11/3.13/3.14 matrix. Effort M.
- **DIRECTION-04** No CLI can degrade or retire knowledge (`DEGRADED`,
  `RETIRED` have no producer). Plan 002 is the first negative-decision
  writer; extend from it after a product decision.
- **DIRECTION-05** A run-independent `--lint` and `--explain-schema` for
  payloads. The redesign plan's own reopening condition has fired; plan 004
  covers the message half, the lint mode remains.
- **DIRECTION-06** Multi-capability ceremonies. Re-measure after 004/005.
- **DEBT-05** Two unreferenced legacy helpers exported from
  `write_authority.py`. Delete. Effort S.

## Findings considered and rejected

- **CORRECTNESS-03** `NOT_SAVED` exits 0 from the finalizer's normal path.
  By design: `SKILL.md:115` defines `NOT_SAVED` as a normal verdict, and the
  exit-2 path is reserved for refusals and infrastructure failures. The
  stdout/stderr split is documented in plan 004 instead.
- **Automatic cleanup of abandoned run markers**: `SKILL.md:109` and the
  payload reference defer this "until observed need"; the real memory shows
  zero open markers, so the precondition has not been met.
- **Family table drift** between `references/target-profile.md` and
  `_FAMILY_FIELDS`: checked key by key, identical.
- **SQL injection**: the only interpolated statement uses a constant table
  name from a fixed dict; every other statement is parameterized.
- **Dry-run leaving partial state**: verified that `_DryRunRollback` rolls
  back under `BEGIN IMMEDIATE` and every early return precedes the transaction.
- **Stale `~/.local/share/caravelaweb/config`**: nothing reads it, the
  documented uninstall removes it, and a test proves resolution ignores it.
- **`${CLAUDE_PLUGIN_ROOT}` naming**: asserted in docs, cannot be verified
  offline; noted, not filed.

## Not audited

- macOS behavior (no CI, no local runner).
- Native Windows behavior beyond reading the CI job and the compat probe tests.
- Real network transports (`agent-browser`, Lightpanda, Chrome) — the audit
  covered the routing and storage code only.
- The Portuguese design note `targets/saindo-do-buraco.md` was read for
  decided tradeoffs only.
